name = "adsgptpro-backend"
main = "src/index.js"
compatibility_date = "2025-01-01"

# Non-secret config (edit these)
[vars]
WITHDRAW_LIMIT_HOURS = "12"
FAUCETPAY_CURRENCY = "USDT"

# D1 database binding â€” create the DB first (see README), then paste its
# database_id below, or let `wrangler d1 create` add this block for you.
[[d1_databases]]
binding = "DB"
database_name = "adsgptpro-db"
database_id = "303beff6-f0aa-43ee-a04f-2415759ff6f2"

# Secrets (do NOT put these here â€” set via dashboard or `wrangler secret put`):
#   FAUCETPAY_API_KEY
#   TELEGRAM_BOT_TOKEN   (must be the AdsGPT Pro bot's own token â€” @Adsgptpro_bot)
